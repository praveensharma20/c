#include <stdio.h>

int main() {
    int c=0,num,i;
    printf("\nEnter the number");
    scanf("%d",&num);
    for(i=1;i<=num;i++)
  {
      if(num%i==0)
      c++;
  }
  if(c==2)
  printf ("\n%dis not a prime",num);
  else
  printf("\n%d is a prime",num);
return 0;
}
